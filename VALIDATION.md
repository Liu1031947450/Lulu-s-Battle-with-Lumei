# 实际验收记录

验收日期：2026 年 9 月 11 日。环境：macOS / Apple Silicon，Node.js 22.22.0，Playwright 1.63.0。

本次在倒计时、中文女声与四档难度设置基础上更新自适应布局：场地按可用宽高等比缩放，短屏压缩留白，长弹窗保留内部滚动。布局改动未涉及角色资产、战斗 / 输入逻辑、五张地图或音频资源。

重新执行构建、原生测试、Chrome / WebKit 完整流程与三内核布局专项。Firefox 完整流程未完成，不能视为全量通过；GLB 标准验证、独立文件复制、三维预览交互、图集边界与 FPS 专项沿用 9 月 10 日验收记录，本次未重跑这些专项。

## 结果概览

| 项目 | 结果 |
| --- | --- |
| Node 核心 / 输入 / 实际 GLB / 语音测试 | **38 / 38 通过** |
| 固定种子人机自动对战 | **100 / 100 正常结束，全部 KO** |
| 模拟统计 | 1604 次有效命中、888 次成功格挡，无异常数值、越界或超时卡死 |
| 四档难度对照模拟 | 400 局全部结束；对同一中等控制器的胜场依次为 0 / 41 / 95 / 100，非真人胜率；中等档逐帧历史哈希与旧版一致 |
| Chrome 153.0.8010.37 | **30 / 30 浏览器流程通过，实际 WebGL 2 渲染** |
| Firefox 155.0（Playwright） | **18 / 18 视口布局专项通过，实际 WebGL 2 渲染**；完整流程未完成 |
| WebKit 26.6（Playwright） | **30 / 30 浏览器流程通过，实际 WebGL 2 渲染** |
| 自适应布局 | 三内核各检查 18 种视口的首页与对战布局，无横向 / 纵向页面溢出；场地等比、关键按钮可见，血条文字和倒计时不重叠 |
| 开场动画与语音 | 321 各一次，第 180 帧开战；暂停冻结动画，静音 / 重开 / 返回首页停止语音，恢复不重播 |
| 语音听感 | 本机 `afplay` 播放完成；主观音色与清晰度仍待用户人工试听确认 |
| 五种背景地图 | 全部绘制成功、缓存不串图、减少动态时画面固定；随机选择覆盖候选，重开不连续重复 |
| 两份 GLB 标准验证 | Khronos glTF Validator：每份均 **0 errors / 0 warnings / 0 infos** |
| 独立单文件验证 | **通过**：仅复制 HTML 到中文 / 空格路径的空目录，仍能离线加载 3D、开始对战和键盘移动 |
| 三维预览 | **通过**：正 / 侧 / 背 / 转台切换，行走逐帧画面变化，暂停后像素保持不变 |
| WebGL / 音频不可用 | Chrome / WebKit 通过关闭 WebGL 和 AudioContext 后的二维回退、倒计时、开局与移动检查；Firefox 本次未重跑此项 |
| 网络与运行时错误 | 本次完成的完整流程与布局专项均无页面错误、无 HTTP/HTTPS 请求 |
| 图集边界 | 两个角色的全部 240 帧均自动检查边缘透明，不裁切旋转与倒地动作 |

被验收成品 `index.html` 的 SHA-256：

```text
8c3638b03a958ccf3f0206cc4d018026453386ae0275cf0c2a0bf69889f8cfda
```

## 覆盖内容

- 模式选择、操作指南、音频解锁与静音、三秒倒计时、对战 HUD。
- 设置提供简单 / 中等 / 困难 / 地狱模式，默认中等；选择持久化，非法存储回退中等，存储被禁用时本页仍可切换。
- 设置暂停倒计时并停止语音，恢复不重播；难度从下一局或各类重开生效，当前局与双人模式不受更改影响。
- 开场第 1 / 61 / 121 帧分别触发三 / 二 / 一，第 180 帧触发开始；真实语音 AudioBuffer 的长度、波形能量与独立 WAV 匹配，字幕 1.1 秒后消失。
- 倒计时与开战横幅的暂停冻结、帮助页、失焦、静音与取消静音、连续重开和返回首页；首次点击解锁音频、空语音表回退原提示音。
- 五张地图和 390px 小窗口下开场数字不遮挡 HUD；小窗口收起额外提示文字，减少动态偏好关闭弹跳与装饰但保留报数。
- 花园、落日海滨、樱花庭院、极光雪原、霓虹天台均可离线绘制；开局与各重开入口选图，对局及暂停恢复不换图，返回首页恢复花园。
- 双方同时移动、双击奔跑、后退、二段跳、长按重复事件过滤、落地重置、下蹲和起身。
- P1 J/K/L、P2 小键盘 1/2/3、主键区 1/2/3 的实际浏览器按键输入；Num Lock 不改变物理 code 的逻辑另有原生测试。
- 普攻前摇、有效命中距离、高度判断、同次攻击只命中一次、双方同帧交换伤害。
- 正面防御减伤、背后不可格挡、空中不可防御、击退、硬直和恢复保护。
- 噜噜突进、噜妹远程泡泡、技能冷却、投射物命中及过期销毁。
- 双人对局通过按键打至 KO；人机对局双方实际命中并正常结算；完整推进 90 秒判平局。
- 暂停冻结时间、冷却与投射物；失焦事件自动暂停；恢复时没有卡住的方向键。
- 结算前打开帮助页不叠加胜负弹窗；再次挑战、R 重开、连续重开与返回主页清理状态。
- 18 种窗口尺寸覆盖桌面、平板、窄屏与横屏，包括 1920×1080、1366×768、1280×720、1024×600、320×568、568×320；检查页面宽高均不溢出、场地比例、按钮边界及最长难度标签。
- 短屏帮助 / 设置弹窗在内部滚动，关闭按钮和完成按钮可访问；设置打开期间缩放窗口不改变帧数、时间、地图、难度或角色状态。
- 减少动态效果偏好、弹窗 Tab / Shift+Tab 顺序、焦点循环与 Escape 关闭。

浏览器测试通过 Playwright 键盘事件操作页面，并用可控时钟逐帧运行游戏；**没有给角色直接改血量、跳过战斗或伪造胜负结果**。`LuluGame.snapshot()` 仅用于读取状态。

## 自制资源检查

- 两个独立 GLB：噜噜 138,768 个三角面，噜妹 260,056 个三角面；每个模型包含完整 16 个关节动画片段，无外部贴图或 `.bin` 依赖。
- 原生测试实际重新载入 GLB，检查关键造型节点、脚底锚点、真实前后体积、材质、有限坐标 / 法线、动画采样与动作关节变化；额外检查换边后使用另一侧出拳关节且不遗留伸长，不通过镜像颠倒胸前徽章。
- 两张 1800×720 三视图与离线预览，均从实际三维网格生成；人工逐视角检查头型、帽壳 / 帽边、圆领、蝴蝶结、鸭子徽章、裤装与圆尾巴。
- 原分层 SVG 保留为 WebGL 不可用时的二维兼容资源，不作为新版三维模型交付。
- 两套 4992×5632 PNG 动作图集；每套 16 种动作、120 帧，每帧 416×352，含逐帧 JSON 坐标与脚底锚点。
- 两张透明立绘、一张 2560×1240 花园背景、一套 8 类特效图集。
- 原有 13 个 WAV 文件保持逐字节一致；新增四段 Tingting 中文合成语音，时长约 0.397 / 0.293 / 0.258 / 0.573 秒，RMS 约 0.14，无削波，首尾淡入淡出。验证 PCM 头、采样率、截断及错误格式拒绝、缓存与播放清理。
- 检查首页、命中、格挡、胜负弹窗、小窗口画面及角色立绘截图。当前机器 Chrome 无头模式下，首页 90 个连续帧的短采样约 60 FPS；这不是其他设备或长时间性能保证。

## 证据与复验

- `artifacts/unit-tests.tap`：原生测试原始输出。
- `artifacts/chromium-report.json`、`artifacts/webkit-report.json`：完整流程逐项结果、浏览器版本、成品 SHA-256、18 种视口几何数据与截图清单。
- `artifacts/firefox-layout-report.json`、`artifacts/webkit-layout-report.json`：当前成品的独立布局、短屏弹窗和缩放专项；`firefox-report.json` 保留全流程中止记录，不作为通过证明。
- `artifacts/chromium-layout-*.png`、`artifacts/layout-final-firefox-*.png`、`artifacts/layout-final-webkit-*.png`：窄屏与横屏布局截图；`artifacts/layout-changes.json` 记录布局修改前后的文件哈希对照。
- `artifacts/chromium-map-*.png`：五张背景的实际画布截图，已逐张检查主题差异及战斗区域留白。
- `artifacts/chromium-countdown-*.png`、`artifacts/chromium-intro-map-*.png`、`artifacts/chromium-small-intro-start.png`：报数、五种背景与小窗口开场截图。
- `artifacts/intro-voice-preview.wav`：按 0 / 1 / 2 / 3 秒排布的语音试听文件；本机已执行 `afplay` 播放，不把命令成功当作人工听感结论。
- `artifacts/assets-report.json`、`artifacts/standalone-report.json`：资源与单文件验证结果。
- `artifacts/lulu-gltf-validation.json`、`artifacts/lumei-gltf-validation.json`：Khronos GLB 校验结果及对应文件 SHA-256。
- `artifacts/models-front.png`、`artifacts/models-side.png`、`artifacts/models-back.png`、`artifacts/standalone-3d.png`：预览交互与独立离线文件截图。
- 项目 ZIP 中的 `verification/` 保存以上报告、原生测试输出和代表性截图；`SHA256SUMS.txt` 列出包内文件校验值。
- 打包脚本拒绝失败报告或旧版成品的浏览器报告，并运行 `unzip -t` 检查压缩包完整性。

```sh
npm test
node tests/browser.mjs
TEST_BROWSER=firefox node tests/browser.mjs
TEST_BROWSER=webkit node tests/browser.mjs
node scripts/export-assets.mjs
node scripts/package.mjs
```

## 验证边界

- 本次交付更新根目录游戏，未重打包此前的 `release/` ZIP；旧压缩包不包含此次难度设置与布局更新。
- 低于 320px 宽度或极端矮屏幕仍可能需要页面滚动；保留最小可用场地区域，不用隐藏溢出来裁切功能。长弹窗在内部滚动，不强行压缩全部内容。
- Firefox 全流程运行较慢，在完成前主动中止；已用当前成品独立完成 18 种视口布局、设置弹窗与暂停缩放专项，不声明完整对局回归通过。
- 三维网格按提供的图片观察重建，不宣称逐像素 / 扫描级一致；采用分部件关节动画，未交付 Humanoid 蒙皮重定向、布料物理或 FBX。具体使用边界见 `assets/models/README.md`。
- WebKit 的离线模拟在本机也会阻止导航到 `file://`。已对照验证：同一文件在未开启模拟时可正常加载。因此 WebKit 测试在导航前阻断全部 HTTP/HTTPS，请求本地文件后立即开启离线模拟，再完成全部对局；游戏没有增加服务器依赖或联网回退。
- WebKit 测试不等于对所有正式 Safari 版本的实机验证；未在 Windows、Android、iOS 或游戏手柄上做实机验收。本版明确面向桌面键盘。
- 失焦保护使用标准 `blur` 事件验证，未逐一验证所有操作系统窗口切换方式；真实键盘的多键冲突也受硬件限制。
- 音频已验证 PCM 数据、实际 AudioBuffer 和播放 / 停止流程，并在本机播放试听文件；当前工具无法提供可读取的听觉反馈，主观音色与清晰度须由用户人工确认。未进行专业设备试听或数小时持续压力测试。
- 不声称绝对零 BUG、官方形象完全一致或官方授权；资源参考与重绘边界详见 `ASSETS.md`。
